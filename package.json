{
  "name": "intuit-interview",
  "private": true,
  "version": "1.0.0",
  "description": "Intuit fraud & risk interview demo — Vercel deploys the React dashboard",
  "scripts": {
    "dev": "npm run dev --prefix intuit-risk-dashboard",
    "build": "npm run build --prefix intuit-risk-dashboard",
    "preview": "npm run preview --prefix intuit-risk-dashboard"
  }
}
